#include <bits/stdc++.h>

#include "macros.h"

using namespace std;

int main() {
#define int ll
  ints(x);
  for (int i = 1;; ++i) {
    if (i * (i + 1) / 2 >= x) {
      wt(i);
      return 0;
    }
  }
}
