#include <bits/stdc++.h>

#include "atcoder.h"
#include "rand.h"

void Main() {
  Random r;
  // int x = r.Int(1, 1000000000000000000LL), y = r.Int(1,
  // 1000000000000000000LL); if (x > y) swap(x, y);
  int x = r.Int(1, 100), y = r.Int(1, 100);
  wt(x, y);
}
