#include <bits/stdc++.h>

#include "atcoder.h"

void Main() {
  V<string> t = {
      ".###..#..###.###.#.#.###.###.###.###.###.",
      ".#.#.##....#...#.#.#.#...#.....#.#.#.#.#.",
      ".#.#..#..###.###.###.###.###...#.###.###.",
      ".#.#..#..#.....#...#...#.#.#...#.#.#...#.",
      ".###.###.###.###...#.###.###...#.###.###.",
  };
  ints(n);
  V<string> s(5);
  cin >> s;
  rep(i, n) {
    rep(j, 10) {
      bool ok = true;
      rep(k, 5) if (s[k].substr(i * 4, 4) != t[k].substr(j * 4, 4)) {
        ok = false;
      }
      if (ok) {
        cout << j;
      }
    }
  }
  cout << endl;
}
