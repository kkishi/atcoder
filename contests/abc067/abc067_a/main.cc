#include <bits/stdc++.h>

#include "macros.h"

using namespace std;

int main() {
  ints(a, b);
  wt((a % 3 == 0 || b % 3 == 0 || (a + b) % 3 == 0) ? "Possible"
                                                    : "Impossible");
}
