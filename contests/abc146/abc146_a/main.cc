#include <bits/stdc++.h>

#include "macros.h"

using namespace std;

int main() {
  vector<string> v = {"SUN", "MON", "TUE", "WED", "THU", "FRI", "SAT"};
  strings(s);
  rep(i, v.size()) if (v[i] == s) {
    wt(7 - i);
    return 0;
  }
}
