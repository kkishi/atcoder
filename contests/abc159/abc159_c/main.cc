#include <bits/stdc++.h>

#define rep(i, n) for (int i = 0; i < (int)(n); ++i)

using namespace std;

int main() {
  double L;
  cin >> L;
  double l = L / 3;
  cout << setprecision(20) << (l * l * l) << endl;
}
