#include <bits/stdc++.h>

#include "macros.h"

using namespace std;

int main() {
#define int ll
  ints(x, y);
  if (x > y) swap(x, y);
  if (y >= 2 + x) {
    wt("Alice");
  } else {
    wt("Brown");
  }
}
